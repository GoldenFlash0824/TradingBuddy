import { Typography } from '@mui/material';

// ==============================|| LOGO SVG ||============================== //

const Logo = () => {
  return (
    <>
      <Typography variant="h3" sx={{ fontSize: '20px', color: '#d1d4dc' }}>
        Trading Buddy
      </Typography>
    </>
  );
};

export default Logo;
